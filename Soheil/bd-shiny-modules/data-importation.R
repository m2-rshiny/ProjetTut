# SHINY UI #####################################################################

dataImportationUI <- function(id) {
  ns <- NS(id)
  tagList(
    # TODO
  )
}

# SERVER LOGIC #################################################################

dataImportation <- function(input, output, session) {
  # TODO
}
