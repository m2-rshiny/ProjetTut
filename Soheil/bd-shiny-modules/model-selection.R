# SHINY UI #####################################################################

model_selection_ui <- function(id) {
  ns <- NS(id)
  tagList(
    h3("Model Selection"),
    hr()
    # TODO
  )
}

# SERVER LOGIC #################################################################

model_selection <- function(input, output, session) {
  # TODO
}
