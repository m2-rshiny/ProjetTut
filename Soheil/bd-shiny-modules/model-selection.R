# SHINY UI #####################################################################

modelSelectionUI <- function(id) {
  ns <- NS(id)
  tagList(
    # TODO
  )
}

# SERVER LOGIC #################################################################

modelSelection <- function(input, output, session) {
  # TODO
}
