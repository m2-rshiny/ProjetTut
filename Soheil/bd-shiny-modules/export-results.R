# SHINY UI #####################################################################

export_results_ui <- function(id) {
  ns <- NS(id)
  tagList(
    h3("Export Results"),
    hr()
    # TODO
  )
}

# SERVER LOGIC #################################################################

export_results <- function(input, output, session) {
  # TODO
}
