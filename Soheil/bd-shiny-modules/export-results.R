# SHINY UI #####################################################################

exportResultsUI <- function(id) {
  ns <- NS(id)
  tagList(
    # TODO
  )
}

# SERVER LOGIC #################################################################

exportResults <- function(input, output, session) {
  # TODO
}
