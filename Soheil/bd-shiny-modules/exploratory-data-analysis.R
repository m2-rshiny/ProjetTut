# SHINY UI #####################################################################

exploratoryDataAnalysisUI <- function(id) {
  ns <- NS(id)
  tagList(
    # TODO
  )
}

# SERVER LOGIC #################################################################

exploratoryDataAnalysis <- function(input, output, session) {
  # TODO
}
